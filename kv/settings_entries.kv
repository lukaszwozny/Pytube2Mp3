#:kivy 1.10.0
#:import Factory kivy.factory.Factory
#:import ScrollItem scrollitem.ScrollItem
#:import SettingsEntries ui.SettingsEntries.SettingsEntries
#:import DotsButton ui.Buttons.BrowseButton
#:import DotsButton ui.Buttons.DotsButton
#:import ConfigManager ConfigManager.ConfigManager

<MyBoxLayout@BoxLayout>:
    id: url_box
    orientation: 'horizontal'
    height: 50
    size_hint_y: None
    padding: 0, 0, 0, 10

<SettingsEntries>:
    orientation: 'vertical'
    padding: 10, 20, 10, 0
    mp3_label: mp3_label

    MyBoxLayout:
        Label:
            id: mp3_label
            text: ConfigManager.config().get('main', 'mp3_location')
            size_hint: .8, 1

        BrowseButton:
            mp3_label: mp3_label
            size_hint: .2, 1

    Widget: