#:kivy 1.10.0
#:import Factory kivy.factory.Factory
#:import SettingsScreen SettingsScreen.SettingsScreen
#:import ScrollItem scrollitem.ScrollItem
#:import SettingsEntries ui.SettingsEntries.SettingsEntries


<SettingsScreen>:
    id: settings_screen
    canvas.before:
        Color:
            rgb: .2, .8, .2
        Rectangle:
            size: self.size
    BoxLayout:
        orientation: 'vertical'
        SettingsEntries:
            id: settings_entries
            save_button: save_button

        BoxLayout:
            Button:
                size_hint: .25, .1
                text: 'Back'
                on_release:
                    ConfigManager.load()
                    settings_entries.update_label()
                    settings_screen.parent.transition = SlideTransition(direction='right')
                    settings_screen.parent.current = 'screen1'
            Button:
                id: save_button
                size_hint: .25, .1
                text: 'Save'
                disabled: True
                on_release:
                    ConfigManager.save()
                    self.disabled = True
